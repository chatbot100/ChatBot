name: Ping Replit

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send request to Replit
        run: |
          curl -s -o /dev/null "https://chatbot_remember.a91324843.repl.co:8080" || echo "Failed to ping"
