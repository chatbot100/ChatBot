name: Ping Replit

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send request to Replit
        run: |
          curl -s -o /dev/null "https://replit.com/@a91324843/ChatBot?v=1" || echo "Failed to ping"
