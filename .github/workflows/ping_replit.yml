name: Ping Replit

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send request to Replit
        run: |
          curl -s -o /dev/null "https://ChatBot?v=1.a91324843.repl.co" || echo "Failed to ping"
